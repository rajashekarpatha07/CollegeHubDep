{
  "name": "college-hub",
  "version": "1.0.0",
  "parse": {
    "appId": "${APP_ID}",
    "masterKey": "${MASTER_KEY}",
    "serverURL": "${SERVER_URL}"
  },
  "scripts": {
    "start": "node -r dotenv/config --experimental-json-modules parse-server.js"
  }
} 